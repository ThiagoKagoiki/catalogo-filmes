import React from "react";
import Nav from "../Componentes/Nav";
import { MovieList } from "../Componentes/MovieList";

export const Home = () => {
    return(
        <div>
            <Nav/>
        </div>
    )
}