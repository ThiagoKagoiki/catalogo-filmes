import React from "react";
import Nav from "../Componentes/Nav";
import { MovieCard } from "../Componentes/MovieCard";

export const Home = () => {
    return(
        <div>
            <Nav/>
            <MovieCard/>
        </div>
    )
}